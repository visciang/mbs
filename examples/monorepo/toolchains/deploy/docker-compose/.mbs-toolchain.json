{
    "id": "toolchain-deploy-docker_compose",
    "toolchain": {
        "dockerfile": "Dockerfile",
        "files": [
            "deploy.sh"
        ],
        "steps": [
            "deploy"
        ]
    }
}