{
    "id": "toolchain-deploy-docker-compose",
    "toolchain": {
        "dockerfile": "Dockerfile",
        "files": [
            "deploy.sh"
        ],
        "steps": [
            "deploy"
        ]
    }
}